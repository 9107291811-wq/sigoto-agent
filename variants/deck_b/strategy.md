# Deck B Strategy

Deck B is ready for a separate list and separate strategy.

Replace this file after the new recipe arrives. The intended workflow is:

1. Put the new 60-card recipe in `decklist.json`.
2. Resolve that list into this folder's `deck.csv`.
3. Add any B-only play rules here.
4. Build `dist/deck_b/submission.tar.gz`.
